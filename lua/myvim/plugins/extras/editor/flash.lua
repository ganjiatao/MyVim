require("lazy.core.util").warn(
  "`flash.nvim` is now the default jump plugin for **MyVim**.\nPlease remove the `myvim.plugins.extras.editor.flash` import from your **lazy** config.",
  { title = "MyVim" }
)

return {}
